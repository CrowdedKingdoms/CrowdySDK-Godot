mutation UpdateOrgToken($orgTokenId: BigInt!, $input: UpdateOrgTokenInput!) {
  updateOrgToken(orgTokenId: $orgTokenId, input: $input) {
    orgTokenId
    label
    isActive
    expiresAt
    revokedAt
    updatedAt
  }
}
