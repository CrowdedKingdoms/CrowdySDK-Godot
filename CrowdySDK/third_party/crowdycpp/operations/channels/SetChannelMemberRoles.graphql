mutation SetChannelMemberRoles($input: SetMemberRolesInput!) {
  setChannelMemberRoles(input: $input) {
    groupMemberId
    groupId
    userId
    status
    createdAt
    roles {
      groupRoleId
      roleName
      rank
      isSystem
      permissions
    }
  }
}
