query ChannelMembers($groupId: BigInt!) {
  channelMembers(groupId: $groupId) {
    groupMemberId
    groupId
    userId
    status
    createdAt
    roles {
      groupRoleId
      roleName
      rank
      isSystem
      permissions
    }
  }
}
